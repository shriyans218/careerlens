# Artifact Card: dashboard_projection.joblib

- **Type**: fitted scaler + PCA(2)/t-SNE-derived 2D projection of the CPDS training set
- **Not a predictive model** — places a resume's trait vector into the dashboard's visualization space
- **Generated by**: `training/09_export_dashboard_data.py`
- **Optional**: prediction endpoints work with or without it; only the dashboard embedding degrades gracefully.
